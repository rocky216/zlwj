class MailListAction {
  List mail;
  
  MailListAction(this.mail);
}

class UserInfoAction {
  Map user;
  UserInfoAction(this.user);
}

class NewsListAction {
  Map news;
  NewsListAction(this.news);
}